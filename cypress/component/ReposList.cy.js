// import ReposList from '../../app/components/ReposList'

// describe('ReposList.cy.js', () => {
//   it('ReposList mount', () => {
//     cy.mount(<ReposList />)
//   })
// })

import Button from '../../app/components/Button';

describe('ReposList.cy.js', () => {
  it('ReposList mount', () => {
    cy.mount(<Button />);
  });
});
